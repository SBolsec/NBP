Trajanje upita: 0.06 sekunda

db.getCollection('watches').aggregate([
    {
        $group: {
            _id: "$product_id",
            numOfPrices: { $sum: 1 }
        }
    },
    {
        $match: { "numOfPrices": { $gt: 1 } }
    }
])
